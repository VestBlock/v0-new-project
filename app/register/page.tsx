import JoinPage from '@/app/join/page'

export const dynamic = 'force-dynamic'

// Keep legacy links and saved bookmarks working while /join remains the
// canonical universal account entry point.
export default JoinPage
